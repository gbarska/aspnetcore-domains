using PaymentContext.Domain.Queries;

namespace PaymentContext.Domain.Repositories
{
    public class StudentRepository
    {
        
        //  public List<Product> GetStudentsInfo(int skip, int take)
        // {
        //     return _context.Students.Where(StudentQueries.GetStudentInfo()).ToList();
        // }
    }
}